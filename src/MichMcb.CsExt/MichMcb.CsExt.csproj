<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net6.0;net5.0;netstandard2.0</TargetFrameworks>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <RootNamespace>MichMcb.CsExt</RootNamespace>
    <Version>2.1.0</Version>
    <Description>A library full of helpful stuff. It's just a collection of functions I've created. This particular library is mainly stuff that I make for myself that I feel like sharing.</Description>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageIcon>Ghost.png</PackageIcon>
    <RepositoryUrl>https://github.com/Michmcb/CsExt</RepositoryUrl>
    <PackageReleaseNotes>- Fixed bug with splitting a string into Ranges, also it now works on a callback providing ReadOnlySpan slices rather than returning a collection of Range
		 - IAsyncEvent, which allows you to have events that return a Task.
		 - Extensions for DateOnly
		 - UtcDateTime.ToString() methods will clamp the resultant string between 0001-01-01 00:00:00 and 9999-12-31 59:99:99.999, if the string is formatted as non-UTC and the requested timezone would offset it outside of that range
		 - UtcDateTime.FromDateTime() as an alternative to casting. It also allows you to treat DateTimeKind.Unspecified the same as DateTimeKind.Utc
		 - ErrMsg struct, to encapsulate an error code and error message.
	 </PackageReleaseNotes>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <PackageProjectUrl>https://github.com/Michmcb/CsExt</PackageProjectUrl>
    <RepositoryType>git</RepositoryType>
    <Authors>Michael McBride</Authors>
    <Product />
    <Company />
    <Copyright></Copyright>
    <AssemblyVersion>2.1.0.0</AssemblyVersion>
    <FileVersion>2.1.0.0</FileVersion>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\..\LICENSE">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
    <None Include="Ghost.png">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="System.Memory" Version="4.5.4" Condition="'$(TargetFramework)'=='netstandard2.0'" />
  </ItemGroup>
</Project>
